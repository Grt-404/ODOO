<!DOCTYPE html>
<html data-wf-domain="www.stockmaster.app" data-wf-page="67a323a1c68594827fa0ccb0" data-wf-site="67a323a1c68594827fa0cca9" lang="en">
<head>
    <meta charset="utf-8"/>
    <title>StockMaster | Dashboard</title>
    <meta content="width=device-width, initial-scale=1" name="viewport"/>
    
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/css/soscale-39349c-c66c87b5666753b1b89d5bc1.webflow.shared.1aa2f82cd.css" rel="stylesheet" type="text/css"/>
    <link href="https://fonts.googleapis.com" rel="preconnect"/>
    <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin="anonymous"/>
    <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js" type="text/javascript"></script>
    <script type="text/javascript">WebFont.load({  google: {    families: ["Montserrat:100,100italic,200,200italic,300,300italic,400,400italic,500,500italic,600,600italic,700,700italic,800,800italic,900,900italic","Oswald:200,300,400,500,600,700"]  }});</script>
    <script type="text/javascript">!function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch&&c instanceof DocumentTouch)&&(n.className+=t+"touch")}(window,document);</script>
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a392515bb29399f8c12d07_fav.png" rel="shortcut icon" type="image/x-icon"/>
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a323a1c68594827fa0ce4b_fav_256.png" rel="apple-touch-icon"/>
    
    <link href="https://cdn.jsdelivr.net/npm/splitting@1.0.6/dist/splitting.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js" defer ></script>
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/gh/timothydesign/script/split-type.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
        /* --- Utilities --- */
        .w-webflow-badge { display: none !important; } 
        html.lenis { height: auto; }
        .lenis.lenis-smooth { scroll-behavior: auto; }
        .heading-scroll .char { transform: translateZ(0) !important; }
        
        /* Letter Wrap Animation */
        .letter-wrap { display: inline-block; position: relative; } 
        .letter-wrap * { pointer-events: none; } 
        .letter-wrap__word { display: inline-flex; position: relative; overflow: hidden; } 
        .letter-wrap__char { display: inline-block; position: relative; text-transform: uppercase; } 
        .letter-wrap__char-inner { display: inline-block; } 
        .letter-wrap__char-inner:after { content: attr(data-letter); position: absolute; top: 100%; left: 0; }

        /* --- DASHBOARD LAYOUT --- */
        .dashboard-section { 
            padding-top: 160px; 
            padding-bottom: 100px; 
            min-height: 100vh; 
            background-color: #0e0e0e; 
            color: #edf2f4; 
            position: relative;
            z-index: 2;
            background-image: radial-gradient(circle at 50% 50%, #1a1a1a 0%, #0e0e0e 100%);
        }

        /* --- IMPROVED HEADER --- */
        .dashboard-nav {
            position: fixed;
            top: 0; left: 0; right: 0;
            height: 90px;
            padding: 0 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(14, 14, 14, 0.7);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .nav-left { display: flex; align-items: center; gap: 30px; }
        
        /* New Logo Styling */
        .nav-logo-text {
            font-family: 'Oswald', sans-serif;
            font-size: 24px;
            color: #fff;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 500;
            text-decoration: none;
        }

        .nav-center {
            display: flex;
            gap: 10px;
            background: rgba(255, 255, 255, 0.03);
            padding: 5px;
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .nav-pill {
            padding: 8px 20px;
            border-radius: 20px;
            color: #888;
            text-decoration: none;
            font-family: 'Montserrat', sans-serif;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .nav-pill:hover { color: #fff; background: rgba(255, 255, 255, 0.1); }
        .nav-pill.active { color: #000; background: #fff; font-weight: 600; }

        .nav-right { display: flex; align-items: center; gap: 20px; }
        
        .user-status {
            display: flex;
            align-items: center;
            gap: 8px;
            font-family: 'Oswald', sans-serif;
            font-size: 14px;
            color: #fff;
        }
        .status-dot {
            width: 8px; height: 8px;
            background: #4dff88;
            border-radius: 50%;
            box-shadow: 0 0 8px #4dff88;
            animation: pulse 2s infinite;
        }
        @keyframes pulse { 0% { opacity: 0.5; } 50% { opacity: 1; } 100% { opacity: 0.5; } }

        .logout-btn {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            border: 1px solid rgba(255,255,255,0.2);
            border-radius: 4px;
            color: #fff;
            text-decoration: none;
            font-family: 'Oswald', sans-serif;
            text-transform: uppercase;
            font-size: 12px;
            transition: all 0.3s;
        }
        .logout-btn:hover { background: #fff; color: #000; border-color: #fff; }

        /* Ticker Styling */
        .ticker-wrapper {
            position: fixed;
            top: 90px; /* Below header */
            width: 100%;
            background: rgba(0,0,0,0.8);
            border-bottom: 1px solid rgba(255,255,255,0.05);
            overflow: hidden;
            white-space: nowrap;
            z-index: 5;
            padding: 6px 0;
        }
        .ticker-text {
            display: inline-block;
            font-family: 'Montserrat', sans-serif;
            font-size: 10px;
            text-transform: uppercase;
            color: #4dff88;
            letter-spacing: 1px;
            animation: ticker 40s linear infinite;
        }
        @keyframes ticker { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

        .dashboard-container {
            max-width: 1400px;
            width: 95vw;
            margin: 40px auto 0;
            display: grid;
            grid-template-columns: 240px 1fr;
            gap: 40px;
        }

        @media (max-width: 991px) {
            .dashboard-nav { padding: 0 20px; height: 70px; }
            .nav-center { display: none; } /* Hide center links on mobile */
            .ticker-wrapper { top: 70px; }
            .dashboard-container { grid-template-columns: 1fr; gap: 40px; }
        }

        /* Sidebar */
        .sidebar-menu { border-right: 1px solid rgba(255,255,255,0.1); padding-right: 20px; }
        .sidebar-header {
            font-family: 'Montserrat', sans-serif;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #666;
            margin-bottom: 15px;
            margin-top: 30px;
        }
        .sidebar-link {
            display: block;
            font-family: 'Oswald', sans-serif;
            font-size: 18px;
            text-transform: uppercase;
            color: #888;
            text-decoration: none;
            padding: 10px 0;
            transition: all 0.3s ease;
            position: relative;
        }
        .sidebar-link:hover, .sidebar-link.active { color: #fff; padding-left: 10px; }
        .sidebar-link.active::before {
            content: '';
            position: absolute; left: 0; top: 50%; transform: translateY(-50%);
            width: 4px; height: 4px; background: #4dff88; border-radius: 50%;
        }

        /* KPI & Content Cards */
        .content-grid-top { display: grid; grid-template-columns: 2fr 1fr; gap: 30px; margin-bottom: 40px; }
        @media (max-width: 767px) { .content-grid-top { grid-template-columns: 1fr; } }

        .kpi-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; }
        @media (max-width: 479px) { .kpi-grid { grid-template-columns: 1fr; } }

        .kpi-card {
            background: #121212;
            border: 1px solid rgba(255,255,255,0.1);
            padding: 25px;
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        .kpi-card::before {
            content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at top right, rgba(255,255,255,0.05), transparent 60%);
            opacity: 0; transition: opacity 0.3s;
        }
        .kpi-card:hover {
            transform: translateY(-5px);
            border-color: rgba(255,255,255,0.3);
            box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5);
        }
        .kpi-card:hover::before { opacity: 1; }
        
        .kpi-value { font-family: 'Oswald', sans-serif; font-size: 48px; line-height: 1; color: #fff; margin-bottom: 5px; }
        .kpi-label {
            font-family: 'Montserrat', sans-serif; font-size: 12px; color: #888;
            text-transform: uppercase; display: flex; align-items: center; gap: 5px;
        }
        .kpi-icon { width: 8px; height: 8px; background: #333; border-radius: 50%; }
        .kpi-icon.active { background: #4dff88; box-shadow: 0 0 8px #4dff88; }
        .kpi-icon.alert { background: #ff4d4d; box-shadow: 0 0 8px #ff4d4d; }

        /* Chart & Terminal */
        .chart-card {
            background: #121212; border: 1px solid rgba(255,255,255,0.1);
            padding: 20px; display: flex; flex-direction: column;
            align-items: center; justify-content: center; position: relative;
        }
        .chart-percent { font-family: 'Oswald', sans-serif; font-size: 32px; color: #fff; }

        .terminal-card {
            background: #080808; border: 1px solid rgba(255,255,255,0.15);
            padding: 0; font-family: 'Courier New', monospace;
        }
        .terminal-header {
            background: rgba(255,255,255,0.05); padding: 10px 20px;
            display: flex; gap: 8px; border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        .term-dot { width: 10px; height: 10px; border-radius: 50%; }
        .term-red { background: #ff5f56; } .term-yellow { background: #ffbd2e; } .term-green { background: #27c93f; }
        
        .activity-row {
            padding: 15px 20px; border-bottom: 1px dashed rgba(255,255,255,0.1);
            display: flex; justify-content: space-between; align-items: center; transition: background 0.2s;
        }
        .activity-row:hover { background: rgba(255,255,255,0.02); }
        .op-time { color: #666; font-size: 12px; margin-right: 15px; }
        .op-ref { color: #4dff88; font-weight: bold; }
        .op-detail { color: #ccc; font-size: 14px; }
        .op-status { font-size: 10px; text-transform: uppercase; padding: 2px 6px; background: rgba(255,255,255,0.1); border-radius: 2px; }
        
        /* Welcome Bar */
        .welcome-bar {
            display: flex; justify-content: space-between; align-items: flex-end;
            margin-bottom: 30px; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 20px;
        }
        .time-display { font-family: 'Oswald', sans-serif; font-size: 40px; color: #fff; line-height: 1; text-align: right; }
        .date-display { font-family: 'Montserrat', sans-serif; font-size: 12px; color: #888; text-transform: uppercase; text-align: right; }

        .cta-btn {
            display: inline-block; margin-top: 20px; padding: 12px 25px; border: 1px solid #fff;
            color: #fff; font-family: 'Oswald', sans-serif; text-transform: uppercase; text-decoration: none;
            transition: 0.3s; background: transparent;
        }
        .cta-btn:hover { background: #fff; color: #000; box-shadow: 0 0 15px rgba(255,255,255,0.3); }
    </style>
</head>

<body class="body">

    <div data-w-id="edddb9d9-c6c4-45f3-fed7-241709972d7b" class="preloader-parent">
        <div class="logoname-wrapper">
            <div class="black-shape-top"></div>
            <div class="black-shape-logo-bot">
                <img src="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a323a1c68594827fa0cd29_SOSCALE-logo2%201.svg" loading="lazy" alt="" class="logo-preloader-color"/>
            </div>
        </div>
    </div>

    <nav class="dashboard-nav">
        <div class="nav-left">
            <a href="/" class="w-inline-block" style="text-decoration: none;">
                <div class="nav-logo-text">Stock Master</div>
            </a>
        </div>

        <div class="nav-center">
            <a href="/home" aria-current="page" class="nav-pill active">
                <span class="letter-wrap">Dashboard</span>
            </a>
            <a href="/products" class="nav-pill">
                <span class="letter-wrap">Products</span>
            </a>
            <a href="/operations" class="nav-pill">
                <span class="letter-wrap">Operations</span>
            </a>
            <a href="/warehouses/map" class="nav-pill">
                <span class="letter-wrap">Map</span>
            </a>
        </div>

        <div class="nav-right">
            <div class="user-status">
                <div class="status-dot"></div>
                <span>Live</span>
            </div>
            <a href="/users/logout" class="logout-btn">
                Logout
                <img src="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a323a1c68594827fa0ccb4_Vector.svg" width="12" alt="" style="filter: invert(1);">
            </a>
        </div>
    </nav>

    <div class="ticker-wrapper">
        <div class="ticker-text">
            SYSTEM STATUS: OPERATIONAL &nbsp;&nbsp;//&nbsp;&nbsp; SYNC: ACTIVE &nbsp;&nbsp;//&nbsp;&nbsp; DATABASE: CONNECTED &nbsp;&nbsp;//&nbsp;&nbsp; WAREHOUSE A: 98% CAPACITY &nbsp;&nbsp;//&nbsp;&nbsp; NEW FEATURES AVAILABLE &nbsp;&nbsp;//&nbsp;&nbsp; 
            SYSTEM STATUS: OPERATIONAL &nbsp;&nbsp;//&nbsp;&nbsp; SYNC: ACTIVE &nbsp;&nbsp;//&nbsp;&nbsp; DATABASE: CONNECTED &nbsp;&nbsp;//&nbsp;&nbsp; WAREHOUSE A: 98% CAPACITY &nbsp;&nbsp;//&nbsp;&nbsp; NEW FEATURES AVAILABLE &nbsp;&nbsp;//&nbsp;&nbsp; 
        </div>
    </div>

    <section class="dashboard-section">
        <div class="dashboard-container">
            
            <aside class="sidebar-menu">
                <div class="sidebar-header">User Profile</div>
                <div style="color: #fff; font-family:'Oswald'; font-size: 20px; margin-bottom: 5px;">
                    <%= typeof user !== 'undefined' ? user.fullname : 'Administrator' %>
                </div>
                <div style="color: #666; font-size: 12px; margin-bottom: 20px;">Level: Admin Access</div>
                
                <a href="/profile" class="sidebar-link">My Profile</a>
                
                <div class="sidebar-header">Quick Actions</div>
                <a href="/operations" class="sidebar-link">Incoming Receipts</a>
                <a href="/operations" class="sidebar-link">Delivery Orders</a>
                <a href="/products" class="sidebar-link">Check Stock</a>
                
                <div class="sidebar-header">Modules</div>
                <a href="/warehouses/map" class="sidebar-link" style="color: #4dff88;">📍 Live Map</a>
            </aside>

            <div class="main-content">
                
                <div class="welcome-bar">
                    <div>
                        <h1 class="heading-scroll _9 size-28" style="font-size: 40px; margin: 0;">Overview</h1>
                        <div style="color: #888; font-family: 'Montserrat'; font-size: 14px; margin-top: 5px;">
                            Real-time insights for your warehouse operations.
                        </div>
                    </div>
                    <div class="time-widget">
                        <div class="time-display" id="live-time">00:00:00</div>
                        <div class="date-display" id="live-date">Loading...</div>
                    </div>
                </div>

                <div class="content-grid-top">
                    <div class="kpi-grid">
                        <div class="kpi-card">
                            <div class="kpi-label"><div class="kpi-icon active"></div> Total SKUs</div>
                            <div class="kpi-value"><%= totalProducts %></div>
                            <div style="font-size: 10px; color: #666;">ACTIVE IN DATABASE</div>
                        </div>
                        <div class="kpi-card">
                            <div class="kpi-label">
                                <div class="kpi-icon <%= lowStockCount > 0 ? 'alert' : 'active' %>"></div> 
                                Low Stock
                            </div>
                            <div class="kpi-value" style="color: <%= lowStockCount > 0 ? '#ff4d4d' : '#fff' %>;">
                                <%= lowStockCount %>
                            </div>
                            <div style="font-size: 10px; color: #666;">ACTION REQUIRED</div>
                        </div>
                        <div class="kpi-card">
                            <div class="kpi-label"><div class="kpi-icon" style="background: #ffbd2e;"></div> Pending Receipts</div>
                            <div class="kpi-value"><%= pendingReceipts %></div>
                            <div style="font-size: 10px; color: #666;">AWAITING PROCESSING</div>
                        </div>
                        <div class="kpi-card">
                            <div class="kpi-label"><div class="kpi-icon" style="background: #ffbd2e;"></div> Pending Deliveries</div>
                            <div class="kpi-value"><%= pendingDeliveries %></div>
                            <div style="font-size: 10px; color: #666;">TO BE SHIPPED</div>
                        </div>
                    </div>

                    <div class="chart-card">
                        <h3 class="sidebar-header" style="margin-top: 0; align-self: flex-start;">Inventory Health</h3>
                        <div style="width: 160px; height: 160px; position: relative;">
                            <canvas id="stockChart"></canvas>
                        </div>
                        <div style="margin-top: 20px; text-align: center;">
                            <div style="font-family: 'Montserrat'; font-size: 12px; color: #888;">Stock Efficiency</div>
                            <div class="chart-percent" id="efficiency-score">--%</div>
                        </div>
                    </div>
                </div>

                <h2 class="sidebar-header" style="font-size: 14px; margin-bottom: 15px;">> System_Logs / Recent_Activity</h2>
                
                <div class="terminal-card">
                    <div class="terminal-header">
                        <div class="term-dot term-red"></div>
                        <div class="term-dot term-yellow"></div>
                        <div class="term-dot term-green"></div>
                        <div style="margin-left: auto; font-size: 10px; color: #666;">bash --v 1.0</div>
                    </div>
                    
                    <% if (recentOps && recentOps.length > 0) { %>
                        <% recentOps.forEach(function(op) { %>
                            <div class="activity-row">
                                <div>
                                    <span class="op-time">[<%= new Date(op.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %>]</span>
                                    <span class="op-ref"><%= op.reference %></span>
                                    <span class="op-detail"> 
                                        :: <%= op.type.toUpperCase() %> 
                                        <% if (op.items && op.items.length > 0 && op.items[0].product) { %>
                                            >> <%= op.items[0].quantity %>x <%= op.items[0].product.name %>
                                        <% } %>
                                    </span>
                                </div>
                                <span class="op-status" style="color: <%= op.status === 'Done' ? '#4dff88' : '#ffbd2e' %>">
                                    <%= op.status %>
                                </span>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div style="padding: 20px; color: #666; text-align: center;">
                            > No logs found. System waiting for input...
                        </div>
                    <% } %>
                </div>

                <div style="text-align: right;">
                    <a href="/operations" class="cta-btn">Full Log Access</a>
                </div>

            </div>
        </div>
    </section>

    <section class="footer">
        <div class="footer-wrapper-top">
            <div class="footer-divider"></div>
            <div class="div-block-75">
                <div class="text-block-7">StockMaster IMS v1.0</div>
                <a href="#" class="text-block-7-copy">© 2025 StockMaster</a>
            </div>
        </div>
    </section>

    <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=67a323a1c68594827fa0cca9" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/js/webflow.schunk.9cd7b152627b8cd1.js" type="text/javascript"></script>
    <script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@1.0.23/bundled/lenis.min.js"></script>

    <script>
        function updateTime() {
            const now = new Date();
            document.getElementById('live-time').innerText = now.toLocaleTimeString([], { hour12: false });
            document.getElementById('live-date').innerText = now.toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' });
        }
        setInterval(updateTime, 1000);
        updateTime();
    </script>

    <script>
        const ctx = document.getElementById('stockChart').getContext('2d');
        const lowStock = <%= lowStockCount %>;
        const total = <%= totalProducts %>;
        const healthy = Math.max(0, total - lowStock);
        
        const score = total > 0 ? Math.round((healthy / total) * 100) : 100;
        document.getElementById('efficiency-score').innerText = score + "%";

        new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: ['Healthy', 'Low Stock'],
                datasets: [{
                    data: [healthy, lowStock],
                    backgroundColor: ['#4dff88', '#ff4d4d'],
                    borderWidth: 0,
                    hoverOffset: 4
                }]
            },
            options: {
                cutout: '75%',
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: { display: false },
                    tooltip: { backgroundColor: '#000', borderColor: '#333', borderWidth: 1 }
                }
            }
        });
    </script>

    <script>
        // Letter Wrap (Hover Effect)
        const letterWrapClass = 'letter-wrap';
        const letterWrapElements = document.getElementsByClassName(letterWrapClass);
        [...letterWrapElements].forEach(el => {
            letterWrap(el, letterWrapClass);
            letterAnimation(el, letterWrapClass);
        });

        function letterWrap(el, cls) {
            const words = el.textContent.split(' ');
            const letters = [];
            cls = cls || 'letter-wrap'
            words.forEach(word => {
                let html = '';
                for (var letter in word) {
                    html += `<span class="${cls}__char"><span class="${cls}__char-inner" data-letter="${word[letter]}">${word[letter]}</span></span>`;
                };
                let wrappedWords = `<span class="${cls}__word">${html}</span>`;
                letters.push(wrappedWords);
            });
            return el.innerHTML = letters.join(' ');
        }

        function letterAnimation(el, cls) {
            const tl = new TimelineMax({ paused: true });
            const characters = el.querySelectorAll(`.${cls}__char-inner`);
            el.animation = tl.staggerTo(characters, 0.3, { y: '-100%', delay: 0.05, ease: Power2.easeOut }, 0.025);
            el.addEventListener('mouseenter', (event) => event.currentTarget.animation.play());
            el.addEventListener('mouseout', (event) => el.animation.pause(0));
        }

        // Smooth Scroll
        if (window.innerWidth > 780) {
            const lenis = new Lenis({ lerp: 0.1, smooth: true });
            function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
            requestAnimationFrame(raf);
        }
    </script>
</body>
</html>