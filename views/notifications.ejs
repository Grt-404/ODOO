<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <title>StockMaster | Notifications</title>
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/css/soscale-39349c-c66c87b5666753b1b89d5bc1.webflow.shared.1aa2f82cd.css" rel="stylesheet" type="text/css"/>
    <link href="https://fonts.googleapis.com" rel="preconnect"/>
    <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin="anonymous"/>
    <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js" type="text/javascript"></script>
    <script type="text/javascript">WebFont.load({ google: { families: ["Montserrat:100,100italic,200,200italic,300,300italic,400,400italic,500,500italic,600,600italic,700,700italic,800,800italic,900,900italic","Oswald:200,300,400,500,600,700"] }});</script>
    
    <style>
        /* Layout */
        .dashboard-section { padding-top: 120px; padding-bottom: 60px; min-height: 100vh; background-color: #0e0e0e; color: white; }
        .dashboard-grid { display: grid; grid-template-columns: 250px 1fr; gap: 40px; max-width: 1400px; margin: 0 auto; padding: 0 20px; }
        
        /* Sidebar */
        .sidebar-menu { border-right: 1px solid rgba(255,255,255,0.1); padding-right: 20px; }
        .sidebar-item { display: block; padding: 15px 0; color: #888; text-decoration: none; font-family: 'Oswald', sans-serif; text-transform: uppercase; font-size: 16px; border-bottom: 1px solid rgba(255,255,255,0.05); transition: 0.3s; }
        .sidebar-item:hover { color: #fff; padding-left: 5px; }
        .sidebar-item.active { color: #4dff88; border-left: 2px solid #4dff88; padding-left: 15px; }

        /* Notification List */
        .notif-container { background: #1a1a1a; border: 1px solid rgba(255,255,255,0.1); padding: 0; }
        .notif-item { 
            display: flex; justify-content: space-between; align-items: center; 
            padding: 20px; border-bottom: 1px solid rgba(255,255,255,0.05); transition: 0.2s;
        }
        .notif-item:hover { background: rgba(255,255,255,0.02); }
        .notif-icon { width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 18px; margin-right: 20px; }
        
        .type-info { background: rgba(77, 166, 255, 0.1); color: #4da6ff; }
        .type-warning { background: rgba(255, 170, 77, 0.1); color: #ffaa4d; }
        .type-success { background: rgba(77, 255, 136, 0.1); color: #4dff88; }
        
        .clear-btn { background: transparent; border: 1px solid #ff4d4d; color: #ff4d4d; padding: 8px 16px; font-family: 'Oswald'; text-transform: uppercase; cursor: pointer; font-size: 12px; transition: 0.3s; }
        .clear-btn:hover { background: #ff4d4d; color: white; }

        .w-webflow-badge { display: none !important; } 
    </style>
</head>
<body class="body">

    <nav class="hero-wrap">
        <a href="/" class="header-text-left _1 fixed-right-mob w-inline-block"><div class="letter-wrap black">STOCKMASTER</div></a>
        <div class="header-text-left fixed-left">
            <div class="header-text-left fix-size">
                <a href="/home" class="menu-link-block w-inline-block"><div class="letter-wrap">Dashboard</div></a>
                <a href="/products" class="menu-link-block w-inline-block"><div class="letter-wrap">Products</div></a>
                <a href="/operations" class="menu-link-block w-inline-block"><div class="letter-wrap">Operations</div></a>
            </div>
        </div>
        <div class="header-text-left fixed-center"><div class="hacker-text _1">Notifications</div></div>
        <a href="/users/logout" class="header-text-left _1 fixed-right w-inline-block"><div class="letter-wrap black">LOGOUT</div></a>
    </nav>

    <section class="dashboard-section">
        <div class="dashboard-grid">
            
            <aside class="sidebar-menu">
                <a href="/home" class="sidebar-item" style="margin-bottom: 30px;">← Back to Dashboard</a>
                <h3 class="form-label" style="color:white; font-family:'Oswald'; border-bottom: 1px solid #333; padding-bottom: 10px;">Account Settings</h3>
                <a href="/profile" class="sidebar-item">Personal Details</a>
                <a href="/profile/security" class="sidebar-item">Security</a>
                <a href="/profile/notifications" class="sidebar-item active">Notifications (<%= user.notifications.length %>)</a>
            </aside>

            <div class="main-content">
                
                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
                    <h2 style="font-family: 'Oswald'; font-size: 36px; margin: 0; text-transform: uppercase;">Alerts & Updates</h2>
                    
                    <% if (user.notifications.length > 0) { %>
                        <form action="/profile/notifications/clear" method="POST">
                            <button type="submit" class="clear-btn">Clear All</button>
                        </form>
                    <% } %>
                </div>

                <div class="notif-container">
                    <% if (user.notifications.length > 0) { %>
                        <% user.notifications.forEach(function(notif) { %>
                            <div class="notif-item">
                                <div style="display: flex; align-items: center;">
                                    <div class="notif-icon type-<%= notif.type %>">
                                        <% if(notif.type == 'warning') { %> ! <% } else if(notif.type == 'success') { %> ✓ <% } else { %> i <% } %>
                                    </div>
                                    <div>
                                        <div style="font-family: 'Montserrat'; font-weight: 500; color: white; font-size: 14px;"><%= notif.message %></div>
                                        <div style="font-family: 'Montserrat'; color: #666; font-size: 11px; margin-top: 4px;">
                                            <%= new Date(notif.date).toLocaleString() %>
                                        </div>
                                    </div>
                                </div>
                                <% if(!notif.read) { %>
                                    <div style="width: 8px; height: 8px; background: #4dff88; border-radius: 50%;"></div>
                                <% } %>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div style="padding: 40px; text-align: center; color: #666; font-family: 'Montserrat';">
                            You're all caught up! No new notifications.
                        </div>
                    <% } %>
                </div>

            </div>
        </div>
    </section>
</body>
</html>